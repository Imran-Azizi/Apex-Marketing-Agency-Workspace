/**
 * Content production workspace entry (v3 — manager confirm on versions).
 * Separate module path so browsers cannot reuse a previously immutable-cached
 * chunk of project-ai-assistant.
 */
export {
  ProjectAiAssistant as ProjectContentWorkspace,
  ProjectAiAssistant,
} from "./project-ai-assistant";
